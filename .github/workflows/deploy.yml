name: Build and Deploy

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install & Build
        run: |
          npm install --legacy-peer-deps
          npm run build

      - name: Clear public_html
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.FTP_PASS }}
          port: 65002
          script: rm -rf /home/u979351849/domains/g3stion.com/public_html/*

      - name: Copy dist to public_html
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.FTP_PASS }}
          port: 65002
          source: "dist/"
          target: "/home/u979351849/domains/g3stion.com/public_html"
          strip_components: 1
